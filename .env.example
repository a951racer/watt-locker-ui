# API URL - set to your Heroku API app URL in production
# In development, the Vite proxy handles this (leave unset or use /api)
VITE_API_URL=/api

# For production deployment:
# VITE_API_URL=https://your-watt-locker-api.herokuapp.com/api
